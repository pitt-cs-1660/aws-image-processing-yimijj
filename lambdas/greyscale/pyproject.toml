[project]
name = "greyscale-lambda"
version = "0.1.0"
description = "Lambda function to convert images to greyscale"
requires-python = ">=3.13"
dependencies = [
    "Pillow>=10.0.0",
    "boto3>=1.28.0"
]
